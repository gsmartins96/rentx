module.exports = {
  dependencies: {
    '@nozbe/watermelondb': {
      platforms: {
        ios: null // disable Android platform, other platforms will still autolink if provided
      }
    }
  }
}
